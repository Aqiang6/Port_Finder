# Port Finder

Windows 端口占用查询小工具。

## 用法

直接双击 `find_port.bat` 运行，按提示输入端口号即可查看占用该端口的进程，并可选择结束进程。

也可在命令行中指定端口：

```cmd
find_port.bat 8080
```

## 功能

- 查询指定端口的占用进程（PID、进程名、内存、窗口标题）
- 支持交互式结束进程
- 自动区分 TCP / UDP
